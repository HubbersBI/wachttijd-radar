# Wachttijd-radar backend

FastAPI + uv. Fetches waiting times from the NZa Zorgbeeld open API, normalises them
into SQLite, and serves them.

See `../CLAUDE.md` for the rules and `../planning/PLAN.md` for the shape.
